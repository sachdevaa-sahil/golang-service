{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Go Project",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}",
            "cwd": "${workspaceFolder}"
        }
    ]
}
